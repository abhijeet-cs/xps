## SQL Execution Order Mnemonic
🧠 **F W G H S D O L** 

Pronounce as:
**Few Good Hackers Select Data Ordered Logically**

### Meaning
- **F** = `FROM / JOIN` → choose tables and combine data  
- **W** = `WHERE` → remove unwanted rows early  
- **G** = `GROUP BY` → create groups  
- **H** = `HAVING` → filter groups  
- **S** = `SELECT` → choose columns / calculations  
- **D** = `DISTINCT` → remove duplicates  
- **O** = `ORDER BY` → sort final rows  
- **L** = `LIMIT / OFFSET` → restrict output rows  
